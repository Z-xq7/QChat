# 2026-04-06 工作日志

## 为 4 个 C++ 服务器编写 CMakeLists.txt 并创建部署指南

### 已完成的工作
1. **分析依赖**: 4 个 C++ 服务器共用同一套依赖 — Boost (ASIO/Beast/filesystem/property_tree)、gRPC+protobuf、MySQL Connector/C++ (libmysqlcppconn)、hiredis、jsoncpp、OpenSSL
2. **编写 CMakeLists.txt**: 为 ChatServer、GateServer、StatusServer、ResourceServer 各写了一份 CMakeLists.txt
   - 每个 CMakeLists.txt 都包含 protobuf/gRPC 代码自动生成（通过 protoc + grpc_cpp_plugin）
   - 使用 find_package 查找系统安装的依赖库
   - 在 Linux 上用 jsoncpp 替代 Windows 的 json_vc71_libmtd.lib
   - 移除了 ws2_32.lib 和 Win32_Interop.lib（Linux 不需要）
3. **创建 deploy.sh**: 一键部署脚本，包含依赖安装、编译、systemd 服务配置
4. **编写完整部署指南**: deploy-guide.md，包含从上传代码到启动服务的全流程

### 代码跨平台兼容性
- `_mkdir` / `localtime_s` 等已用 `#if defined(_WIN32)` 条件编译处理，**无需修改**
- `json_vc71_libmtd.lib` 在 CMakeLists.txt 中已替换为 `libjsoncpp`
- config.ini 大小写敏感问题：部署时需确保 key 与代码中读取的一致

### 文件清单
- `ChatServer/CMakeLists.txt` ✅
- `GateServer/CMakeLists.txt` ✅
- `StatusServer/CMakeLists.txt` ✅
- `ResourceServer/CMakeLists.txt` ✅
- `deploy.sh` ✅
- `deploy-guide.md` ✅
